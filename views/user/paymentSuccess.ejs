<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f9fa;
        }
        .success-container {
            text-align: center;
            background: #fff;
            padding: 20px 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .success-icon {
            font-size: 60px;
            color: #28a745;
        }
        .success-message {
            font-size: 24px;
            color: #333;
            margin: 15px 0;
        }
        .order-details {
            font-size: 18px;
            color: #555;
            margin: 10px 0;
        }
        .home-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .home-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="success-container">
        <div class="success-icon">✔</div>
        <div class="success-message">Payment Successful!</div>
        <div class="order-details">
            <p>Thank you for your payment.</p>
            <p>Transaction ID: <strong>#<%=details.paymentId%></strong></p>
            <p>Amount Paid: <strong>$:<%=details.amount%></strong></p>
        </div>
        <a href="/" class="home-btn">Go to Home</a>
    </div>
</body>
</html>
